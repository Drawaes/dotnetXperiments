using System;
using System.Collections.Generic;
using System.Text;

namespace ILSmasher
{
    //public static class CodeSweeper
    //{
    //    public static int ItemsInBacklog => TypeMapper.ItemsInBacklog + GenericMapper.ItemsInBacklog + MethodMapper.ItemsInBacklog;

    //    public static void Iterate(int count)
    //    {
    //        var currentCount = 0;
    //        do
    //        {
    //            currentCount = 0;
    //            if (count > 0)
    //            {
    //                var result = GenericMapper.Iterate(count);
    //                currentCount += result;
    //                count -= result;
    //            }
    //            if (count > 0)
    //            {
    //                var result = TypeMapper.Iterate(count);
    //                currentCount += result;
    //                count -= result;
    //            }
    //            if(count > 0)
    //            {
    //                var result = MethodMapper.Iterate(count);
    //                currentCount += result;
    //                count -= result;
    //            }
    //        } while (count > 0 && currentCount > 0);
    //    }
    //}
}
